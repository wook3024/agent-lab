# Sandbox Instructions

- Work only inside this sandbox.
- Fix stale presence handling with the smallest coherent change.
- Keep tests meaningful and update docs when the protocol changes.
- Run the commands in `benchmark_task.json` before declaring success.
